#Overview
A simple static resume webpage. The main index.html links to each of the 5 pages stored in the pages directory. The website uses the Bootstrap framework, primarily for its grid style, which simplifies the process of structuring the website. Icons and font are provided by FontAwesome and Google Fonts.

#Installation
The website does not use any Javascript files. The website should run as is. Tests were performed on Google Chrome (mobile and Desktop)